import { Routes } from '@angular/router';
import { TopPageComponent } from './feature/top-page/top-page.component';

export const routes: Routes = [
  {
    path: '',
    component: TopPageComponent,
  }
];
